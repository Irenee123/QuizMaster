specVersion: "v1beta"
serviceId: "quizmaster"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "quizmaster-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
